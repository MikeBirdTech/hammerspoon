-- ~/.hammerspoon/config.lua

local config = {
  modules = {
      "windows",
      "colorpicker",
  },
  -- Global key definitions
  keys = {
    meh = {"alt", "ctrl", "shift"},
    hyper = {"alt", "ctrl", "shift", "cmd"}
  },
  -- Add any global configuration options here as needed
}

return config